import SwiftUI

struct SettingsView: View {
    var body: some View {
        Text("Mes réglages")
            .font(.title)
            .fontWeight(.semibold)
    }
}

#Preview {
    SettingsView()
}
