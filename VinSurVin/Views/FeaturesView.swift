import SwiftUI

struct FeaturesView: View {
    var body: some View {
        VStack {
            Text("Fonctionnalités")
                .font(.title)
                .fontWeight(.semibold)
        }
        .padding()
    }
}

#Preview {
    FeaturesView()
}
