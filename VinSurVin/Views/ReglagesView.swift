import SwiftUI
import SwiftData

struct ReglagesView: View {
    
    var body: some View {
        Text("Mes réglages")
            .font(.title)
            .fontWeight(.semibold)
    }
}

#Preview {
    ReglagesView()
}
