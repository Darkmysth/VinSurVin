import SwiftUI

struct FonctionnalitesView: View {
    var body: some View {
        VStack {
            Text("Fonctionnalités")
                .font(.title)
                .fontWeight(.semibold)
        }
        .padding()
    }
}

#Preview {
    FonctionnalitesView()
}
